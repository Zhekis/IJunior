using System;

class Program
{
    static void Main(string[] args)
    {
        /// Task 5
        string givenName = "Zh";
        string firstName = "Akh";
        string fullName = givenName + firstName;
        string givenName1 = givenName;
        givenName = firstName;
        firstName = givenName1;
        string fullName = givenName + firstName;
        Console.WriteLine(fullName);
        Console.WriteLine(fullName1);
    }
}
