using System;

class Program
{
    static void Main(string[] args)
    {
        int allPic = 52;
        int numInRow = 3;
        int fullCountRow = allPic / numInRow;
        int countOver = allPic % numInRow;
        Console.WriteLine(fullCountRow);
        Console.WriteLine(countOver);
    }
}
